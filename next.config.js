/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // If deploying to a subdirectory, add basePath here:
    // basePath: '/your-repository-name', // Replace with your repository name
  };
  
  module.exports = nextConfig;